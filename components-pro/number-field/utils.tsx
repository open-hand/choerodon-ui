import { Utils } from 'choerodon-ui/dataset';

const getNearStepValues: typeof Utils.getNearStepValues = Utils.getNearStepValues;
const MAX_SAFE_INTEGER: typeof Utils.MAX_SAFE_INTEGER = Utils.MAX_SAFE_INTEGER;
const MIN_SAFE_INTEGER: typeof Utils.MIN_SAFE_INTEGER = Utils.MIN_SAFE_INTEGER;
const parseNumber: typeof Utils.parseNumber = Utils.parseNumber;
const parseBigNumber: typeof Utils.parseBigNumber = Utils.parseBigNumber;

export {
  MAX_SAFE_INTEGER,
  MIN_SAFE_INTEGER,
  getNearStepValues,
  parseNumber,
  parseBigNumber,
};
