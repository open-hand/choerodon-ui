export { Size } from 'choerodon-ui/lib/_util/enum';

export enum WaitType {
  throttle = 'throttle',
  debounce = 'debounce',
}

export enum Tooltip {
  none = 'none',
  always = 'always',
  overflow = 'overflow',
}

export enum FieldFocusMode {
  checked = 'checked',
  focus = 'focus',
}
