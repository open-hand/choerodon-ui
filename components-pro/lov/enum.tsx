export enum LovFieldType {
  INT = 'INT',
  TEXT = 'TEXT',
  DATE = 'DATE',
  DATETIME = 'DATETIME',
  POPUP = 'POPUP',
  SELECT = 'SELECT',
}

export enum ViewMode {
  default = 'default',
  button = 'button',
}

export enum PrefixRule {
  start = 'start',
  end = 'end',
  include = 'include',
}

export enum SearchAction {
  blur = 'blur',
  input = 'input',
}
