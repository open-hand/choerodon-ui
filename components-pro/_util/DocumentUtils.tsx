import { Util } from 'choerodon-ui/shared';

export { MousePosition } from 'choerodon-ui/shared/util';

const { getDocument, getDocuments, findIFrame, getMousePosition } = Util;

export {
  getDocument,
  getDocuments,
  findIFrame,
  getMousePosition,
};
