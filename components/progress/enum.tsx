export const enum ProgressType {
  line = 'line',
  circle = 'circle',
  dashboard = 'dashboard',
  loading = 'loading',
}

export const enum ProgressStatus {
  normal = 'normal',
  success = 'success',
  active = 'active',
  exception = 'exception',
}

export const enum ProgressPosition {
  top = 'top',
  bottom = 'bottom',
  left = 'left',
  right = 'right',
}
