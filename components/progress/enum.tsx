export enum ProgressType {
  line = 'line',
  circle = 'circle',
  dashboard = 'dashboard',
  loading = 'loading',
}

export enum ProgressStatus {
  normal = 'normal',
  success = 'success',
  active = 'active',
  exception = 'exception',
}

export enum ProgressPosition {
  top = 'top',
  bottom = 'bottom',
  left = 'left',
  right = 'right',
}
